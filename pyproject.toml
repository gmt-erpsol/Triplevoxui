[project]
name = "triplevox_platform"
authors = [{ name = "TripleVox Engineering PLC", email = "admin@triplevox.com" }]
description = "TripleVox white-label ERP platform shell"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]

[build-system]
requires = ["flit_core >=3.4.0"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]

[project.urls]
Homepage = "https://triplevox.com"
